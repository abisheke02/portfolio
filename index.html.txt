import React, { useState } from "react";
import { motion } from "framer-motion";
import {
  ArrowDownToLine,
  Github,
  Linkedin,
  Mail,
  Phone,
  ExternalLink,
  MapPin,
  Menu,
  X,
  Shield, // For the central hero icon
  Target, // For LLMOps/Security skills
  Brain, // For GenAI skills
  Cloud, // For Cloud skills
  Search, // For RAG/Embeddings
  Zap, // For Automation
  Box, // For MLOps
  Code, // For Tech Stacks
  Database, // For Data
  // --- NEW IMPORTS for Specialized Skills ---
  ShieldCheck, // For Cloud Security
  UserCheck, // For IAM
  MailWarning, // For Phishing Detection
  MessageSquare, // For NLP/VLM
  GraduationCap, // For Education
  Award, // For Certifications
  Users, // For Leadership
} from "lucide-react";

// === Custom Color Definitions for Enhanced Glow ===
// Primary Deep Blue Glow (Indigo 500 RGB)
const DEEP_BLUE_GLOW_RGB = '99, 102, 241';
// Color constants used for static styling (using Tailwind arbitrary values)
const BLUEBONNET_700_RGB = '67, 56, 202'; // Base border color
const BLUEBONNET_400_RGB = '129, 140, 248'; // Icon/Subtitle color
const BLUEBONNET_300_RGB = '165, 180, 252'; // Title color
const SLATE_BG_DARK = '#03080e'; // Deepest background color

// Framer Motion Hover Style for reliable custom glow - APPLIED GLOBALLY TO ALL CARDS
const hoverStyles = {
  scale: 1.02,
  // Layered shadows for an intense, 'electric' dark blue glow
  boxShadow: `0 0 15px rgba(${DEEP_BLUE_GLOW_RGB}, 0.9), 0 0 40px rgba(${DEEP_BLUE_GLOW_RGB}, 0.5)`,
  // Ensure the border also changes to the bright bluebonnet color on hover
  borderColor: `rgb(${DEEP_BLUE_GLOW_RGB})`,
  transition: { duration: 0.3 }
};


// --- Profile Data ---
const profile = {
  name: "ABISHEK ELUMALAI",
  role: "AI/LLM Engineer | MLOps Specialist",
  tagline:
    "Ship privacy-safe, reliable generative AI — from prototype to production.",
  email: "abi.tvm02@gmail.com",
  phone: "+91 70946 07869",
  location: "Tamil Nadu, India",
  links: {
    linkedin: "https://linkedin.com/in/elumalaiabishek",
    github: "https://github.com/abisheke02",
    resume: "/Abishek_Resume.pdf",
  },
};

// --- Skills Data ---
const coreSkills = [
  { label: "LLMOps / MLOps", icon: Target },
  { label: "Generative AI", icon: Brain },
  { label: "Cloud & DevOps", icon: Cloud },
  { label: "RAG / Embeddings", icon: Search },
  { label: "Automation / API", icon: Zap },
  { label: "ML Artifact Mgmt", icon: Box },
];

const techSkills = [
  { title: "LLM & AI Frameworks", items: ["Python", "Hugging Face Transformers", "OpenAI API", "LangChain/LlamaIndex", "Pandas/NumPy"] },
  { title: "Cloud Infrastructure", items: ["AWS (EC2, Lambda, S3, Auto Scaling)", "Linux", "Terraform", "Ansible", "Git/GitHub"] },
  { title: "Data & Storage", items: ["SQL", "MySQL", "FAISS/Vector Stores", "Jupyter/Colab"] },
];

// Specialized Skills (incorporating security/LLM domains)
const specializedSkills = [
  { label: 'Cloud Security', Icon: ShieldCheck },
  { label: 'IAM', Icon: UserCheck },
  { label: 'Threat Hunting', Icon: Target },
  { label: 'Phishing Detection', Icon: MailWarning },
  { label: 'Reinforcement Learning', Icon: Zap },
  { label: 'GenAI & ML', Icon: Brain },
  { label: 'NLP / VLM', Icon: MessageSquare },
  { label: 'SQL', Icon: Database },
];


// --- Projects Data ---
const projects = [
  {
    title: "LLM-Powered Contextual RAG Chatbot (Private Docs)",
    date: "Sep 2024",
    summary:
      "Built a Retrieval-Augmented Generation (RAG) pipeline using a private document corpus. Implemented document chunking, vector embeddings (Sentence Transformers), and FAISS indexing to ground the LLM (Gemini/Llama) for accurate, context-aware responses. Tech: Python, LangChain/LlamaIndex, FAISS, Streamlit.",
    tech: ["Python", "LangChain/LlamaIndex", "FAISS", "Hugging Face", "Streamlit"],
    links: { repo: "#", demo: "#" },
  },
  {
    title: "Phishing Attack Detection in Ethereum Smart Contracts",
    date: "Feb 2024",
    summary:
      "Built a graph-based ML pipeline—Random Walk + Skip-Gram embeddings with supervised classifiers—in Python (scikit-learn, NetworkX) to flag malicious accounts.",
    tech: ["Python", "Graph Embeddings", "scikit-learn", "NetworkX", "Ethereum"],
    links: { repo: "#", demo: "#" },
  },
  {
    title: "Stock Price Prediction using Linear Regression",
    date: "Mar 2023",
    summary:
      "Developed a linear-regression forecaster with engineered time-series features (moving averages, lags, volume) in Python (pandas, NumPy, scikit-learn); evaluated with MAE.",
    tech: ["Python", "Linear Regression", "Pandas", "NumPy", "scikit-learn", "Time Series"],
    links: { repo: "#", demo: "#" },
  },
];

// --- Experience Data ---
const experience = [
  {
    company: "Prochant India Pvt. Ltd.",
    role: "Process Associate (Quality Assurance & Automation)",
    period: "May 2021 – May 2023",
    bullets: [
      "Audited and processed patient intake and insurance eligibility data across diverse EMR/billing systems (e.g., Epic, Allscripts), ensuring 99.5% data integrity.",
      "**Contributed to AI Quality Assurance** by rigorously testing the Parachute AI bot, documenting and reporting document split errors and missed accounts to stabilize pipeline updates.",
      "**Designed and implemented an exceptions tracking workflow** using Google Sheets/Excel (VLOOKUP/Pivot Tables) and SOPs to reconcile bot output against source queues, **reducing turnaround time (TAT) by 15%**.",
    ],
  },
  {
    company: "Online Instructor & Mentor — AI and Automation (Remote)",
    role: "AI/LLM Training & MLOps Pedagogy Specialist",
    period: "2023 – Present",
    bullets: [
      "**Developed and delivered curricula** for live sessions and 1:1 mentorship focused on practical Generative AI, LLMs, Hugging Face Transformers, and low-code automation (n8n).",
      "**Trained learners in fundamental MLOps practices**, including deploying fine-tuned models to Hugging Face Spaces and adopting systematic model evaluation (Precision, Recall, F1) and API authentication (OAuth2).",
      "**Created reusable ML artifacts** (model cards, prompt templates, documented notebooks) and introduced concepts like **SHAP/LIME** for interpretability and structured error analysis to a non-technical audience.",
    ],
  },
];

// --- Education & Certifications Data ---
const education = [
  {
    school: "University College of Engineering Tindivanam, Anna University",
    credential: "B.Tech (Information Technology)",
    year: "2021",
  },
  {
    school: "Gandhinagar Matriculation Higher Secondary School — State Board, Tamil Nadu",
    credential: "Higher Secondary",
    year: "2015",
  },
];

const certifications = [
  "Generative AI with Python: Build Apps in OpenAI & LangChain",
  "Fine‑Tuning LLMs with Hugging Face Transformers (NLP) — 2025",
  "Introduction to Prompt Engineering for Generative AI",
  "Oracle Cloud Infrastructure Generative AI Professional",
];

// --- Small UI helpers ===
const Section = ({ id, title, children }) => (
  <section id={id} className="scroll-mt-24 max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <motion.h2
      // Title: bluebonnet-300 text
      className={`text-2xl sm:text-3xl font-extrabold tracking-tight text-[rgb(${BLUEBONNET_300_RGB})] border-b border-[rgb(${BLUEBONNET_400_RGB})]/50 pb-2`}
      initial={{ opacity: 0, y: 8 }}
      whileInView={{ opacity: 1, y: 0 }}
      viewport={{ once: true, amount: 0.1 }}
      transition={{ duration: 0.4 }}
    >
      {title}
    </motion.h2>
    <div className="mt-6">{children}</div>
  </section>
);

const Pill = ({ children }) => (
  // Pill: bluebonnet-500 border and bluebonnet-200 text
  <span className={`inline-block rounded-full border border-[rgb(${DEEP_BLUE_GLOW_RGB})] text-[rgb(224,231,255)] bg-slate-800/80 px-3 py-1 text-xs leading-6 shadow-md mr-2 mb-2 transition hover:bg-slate-700 font-mono`}>
    {children}
  </span>
);

// SkillIconCard already has hoverStyles applied
const SkillIconCard = ({ label, Icon }) => (
    <motion.div
      // Base classes
      className={`flex flex-col items-center justify-center p-4 rounded-2xl border border-[rgb(${BLUEBONNET_700_RGB})]/60 bg-slate-900/60 shadow-2xl transition duration-300 hover:bg-slate-800/90 cursor-pointer text-center`}
      initial={{ opacity: 0, scale: 0.9 }}
      whileInView={{ opacity: 1, scale: 1 }}
      whileHover={hoverStyles} // Applied custom hover styles (deep blue glow)
      viewport={{ once: true, amount: 0.5 }}
      transition={{ duration: 0.3 }}
    >
      {/* Icon */}
      <Icon className={`h-6 w-6 text-[rgb(${BLUEBONNET_400_RGB})] mb-1`} />
      <p className="text-sm font-semibold text-slate-50 mt-1">{label}</p>
    </motion.div>
);

// Component for Education/Certification/Internship items (Now uses motion.div and hoverStyles)
const DetailBox = ({ title, subtitle, date, children, Icon }) => (
    <motion.div
      // Applied transition and hover styles to the inner DetailBox as requested
      className={`rounded-xl border border-slate-700 bg-slate-900/60 p-4 shadow-lg transition duration-300 h-full cursor-pointer`}
      initial={{ opacity: 0, y: 10 }}
      whileInView={{ opacity: 1, y: 0 }}
      whileHover={hoverStyles} // Added hover style
      viewport={{ once: true, amount: 0.3 }}
      transition={{ duration: 0.4 }}
    >
      <div className="flex items-center gap-3 mb-2 border-b border-slate-700/50 pb-2">
        <Icon className={`h-5 w-5 text-[rgb(${BLUEBONNET_400_RGB})] shrink-0`} />
        <div>
          {/* bluebonnet-300 text */}
          <h4 className={`font-medium text-[rgb(${BLUEBONNET_300_RGB})] text-lg`}>{title}</h4>
          {subtitle && <p className="text-slate-200 text-sm">{subtitle}</p>}
        </div>
        {date && <p className="text-sm text-slate-500 ml-auto shrink-0 mt-1">{date}</p>}
      </div>
      {children && <div className="mt-3 text-sm text-slate-200">{children}</div>}
    </motion.div>
);


// === Main page ===
export default function App() {
  const hasResume = Boolean(profile.links.resume && profile.links.resume !== "#");
  const [isMenuOpen, setIsMenuOpen] = useState(false); // State for mobile menu
  const toggleMenu = () => setIsMenuOpen(!isMenuOpen);

  const navItems = [
    ["About", "#about"],
    ["Skills", "#skills"],
    ["Experience", "#experience"],
    ["Projects", "#projects"],
    ["Education", "#education"],
    ["Contact", "#contact"],
  ];


  return (
    // Body with deeper, higher-contrast background
    <div className={`min-h-screen bg-[${SLATE_BG_DARK}] text-slate-100 font-[Inter] relative`}>
      
      {/* 1. Mobile & Small Screen Sticky Header */}
      <header className={`sticky top-0 z-50 backdrop-blur-sm bg-[${SLATE_BG_DARK}]/70 border-b border-slate-700/50 md:hidden`}>
        <nav className="max-w-6xl mx-auto flex items-center justify-between px-4 sm:px-6 lg:px-8 h-16">
          {/* bluebonnet-300 text */}
          <a href="#home" className={`text-xl font-extrabold text-[rgb(${BLUEBONNET_300_RGB})] hover:text-[rgb(224,231,255)] transition`}>A.E.</a>
          
          <button
            onClick={toggleMenu}
            className="p-2 rounded-full border border-slate-700 text-slate-300 hover:bg-slate-800 transition"
            aria-label="Toggle navigation menu"
          >
            {isMenuOpen ? <X className="h-5 w-5" /> : <Menu className="h-5 w-5" />}
          </button>
        </nav>
      </header>

      {/* Mobile Menu Overlay */}
      <div
        className={`md:hidden fixed inset-0 z-40 bg-slate-900/90 backdrop-blur-sm transition-opacity duration-300 ${
          isMenuOpen ? "opacity-100 pointer-events-auto" : "opacity-0 pointer-events-none"
        }`}
        onClick={toggleMenu}
      >
        <div
          className={`absolute right-0 top-0 h-full w-64 bg-slate-800 border-l border-slate-700 shadow-2xl transform transition-transform duration-300 ${
            isMenuOpen ? "translate-x-0" : "translate-x-full"
          }`}
          onClick={(e) => e.stopPropagation()}
        >
          <div className="p-6 pt-16">
            <div className="space-y-4 text-lg">
              {navItems.map(([label, href]) => (
                <a 
                  key={label} 
                  href={href} 
                  onClick={toggleMenu} 
                  // bluebonnet-400 hover
                  className={`block text-slate-200 hover:text-[rgb(${BLUEBONNET_400_RGB})] transition py-2 border-b border-slate-700/50`}
                >
                  {label}
                </a>
              ))}
            </div>
          </div>
        </div>
      </div>

      {/* 2. Desktop Floating Side Nav */}
      <nav className="fixed right-0 top-1/2 -translate-y-1/2 z-40 hidden md:block w-36 lg:w-44 p-4">
        {/* bluebonnet-700 border */}
        <div className={`bg-slate-900/70 backdrop-blur-sm rounded-xl border border-[rgb(${BLUEBONNET_700_RGB})]/50 p-4 shadow-2xl`}>
          <div className="space-y-3 text-sm font-medium">
            {navItems.map(([label, href]) => (
              <a 
                key={label} 
                href={href} 
                // bluebonnet-400 hover
                className={`block text-slate-300 hover:text-[rgb(${BLUEBONNET_400_RGB})] transition text-right`}
              >
                {label}
              </a>
            ))}
          </div>
        </div>
      </nav>

      {/* Hero (Centered and Striking) */}
      <section id="home" className="relative overflow-hidden pt-24 pb-32 min-h-[600px] flex items-center justify-center text-center">
        {/* Subtle background glow */}
        <div className="absolute inset-0 -z-10 opacity-15" aria-hidden>
          <div className="h-[500px] bg-[rgb(49,46,129)]/60 blur-3xl w-full" />
        </div>

        <div className="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
          <motion.h1
            initial={{ opacity: 0, y: 15 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6 }}
            // --- UPDATED: Name color changed to white ---
            className={`text-6xl sm:text-8xl font-extrabold tracking-tighter text-white`}
          >
            {profile.name}
          </motion.h1>
          <p className={`mt-4 text-xl sm:text-3xl font-medium tracking-wide text-[rgb(${BLUEBONNET_300_RGB})]/90 italic`}>
            {profile.role}
          </p>

          <motion.div
            initial={{ opacity: 0, scale: 0.8 }}
            animate={{ opacity: 1, scale: 1 }}
            transition={{ duration: 0.7, delay: 0.3 }}
            // bluebonnet-500 border
            className={`mt-12 mb-8 relative inline-flex items-center justify-center p-6 rounded-full border-2 border-[rgb(${DEEP_BLUE_GLOW_RGB})]/80 group`}
          >
            {/* bluebonnet-400 icon */}
            <Shield className="h-10 w-10 text-[rgb(129,140,248)] z-10" />
            {/* bluebonnet-500 hover ring */}
            <div className={`absolute inset-0 rounded-full bg-[rgb(${DEEP_BLUE_GLOW_RGB})] opacity-0 group-hover:opacity-15 transition-opacity duration-300 animate-pulse-slow`}></div>
          </motion.div>
          
          <p className="mt-4 max-w-2xl mx-auto text-slate-300 text-lg">
                <span className="font-light">{profile.tagline}</span>
          </p>
          
          <div className="mt-10 flex flex-wrap items-center justify-center gap-4">
              {hasResume && (
              <a
                href={profile.links.resume}
                target="_blank"
                rel="noreferrer"
                download
                // bluebonnet-600 button, bluebonnet-500 shadow
                className={`inline-flex items-center gap-2 rounded-xl bg-[rgb(79,70,229)] text-white px-5 py-2 text-base font-semibold shadow-xl shadow-[rgb(${DEEP_BLUE_GLOW_RGB})]/50 hover:bg-[rgb(${BLUEBONNET_700_RGB})] transition`}
              >
                <ArrowDownToLine className="h-5 w-5" /> Download CV
              </a>
              )}
              <a
                href={profile.links.linkedin}
                target="_blank"
                rel="noreferrer"
                // bluebonnet-300 text, bluebonnet-400 hover border
                className={`inline-flex items-center gap-2 rounded-xl border border-slate-600 text-[rgb(${BLUEBONNET_300_RGB})] px-5 py-2 text-base hover:bg-slate-800 hover:border-[rgb(${BLUEBONNET_400_RGB})] transition`}
              >
                <Linkedin className="h-5 w-5" /> Connect
              </a>
            </div>

        </div>
      </section>

      {/* About */}
      <Section id="about" title="About Me">
        <p className="text-slate-200 max-w-4xl leading-relaxed">
          As an AI/ML Engineer, I specialize in taking **LLM features from prototype to production**. My expertise lies in building clean data and feature pipelines using **Python** and **SQL**, and expertly operationalizing them on **AWS** with rigorous Git practices and automation. I focus on designing efficient prompt and model lifecycles, automating evaluation and monitoring, and partnering with product teams to ship **privacy‑safe, scalable generative AI experiences**.
        </p>
      </Section>

      {/* Skills (Restructured) */}
      <Section id="skills" title="Skills & Technology Stack">
        
        {/* MERGED CONTAINER for Core Focus Areas and Specialized Domains (Hoverable) */}
        <motion.div
            className={`rounded-2xl border-2 border-[rgb(${BLUEBONNET_700_RGB})]/50 bg-slate-900/70 p-6 shadow-2xl flex flex-col transition duration-300 cursor-pointer mb-12`}
            whileHover={hoverStyles}
            initial={{ opacity: 0, y: 10 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true, amount: 0.2 }}
            transition={{ duration: 0.4 }}
        >
            {/* Core Skills (Icon Grid) */}
            <h3 className="text-xl font-semibold text-slate-50 mb-4 mt-0">Core Focus Areas</h3>
            <div className="grid grid-cols-3 sm:grid-cols-4 lg:grid-cols-6 gap-4">
              {coreSkills.map((skill) => (
                <SkillIconCard key={skill.label} label={skill.label} Icon={skill.icon} />
              ))}
            </div>

            {/* Specialized Skills */}
            <h3 className="text-xl font-semibold text-slate-50 mb-4 mt-8 pt-4 border-t border-slate-700/50">Specialized Domains</h3>
            <div className="grid grid-cols-4 sm:grid-cols-6 lg:grid-cols-8 gap-4">
              {specializedSkills.map((skill) => (
                <SkillIconCard key={skill.label} label={skill.label} Icon={skill.Icon} />
              ))}
            </div>
        </motion.div>


        {/* Tech Stack (Pill List Grid) - Hoverable */}
        <h3 className="text-xl font-semibold text-slate-50 mb-4 mt-12">Technical Stack</h3>
        <div className="grid md:grid-cols-3 gap-6">
          {techSkills.map(({ title, items }) => (
            <motion.div 
              key={title} 
              // bluebonnet-700 border
              className={`rounded-2xl border border-slate-700/50 bg-slate-900/60 p-5 shadow-inner transition duration-300 cursor-pointer`}
              whileHover={hoverStyles} // Applied custom hover styles (deep blue glow)
            >
              {/* bluebonnet-400 text */}
              <h4 className={`font-bold text-[rgb(${BLUEBONNET_400_RGB})] mb-3 flex items-center gap-2 border-b border-slate-700/50 pb-2`}>
                <Code className="h-4 w-4" /> {title}
              </h4>
              <div>
                {items.map((s) => (
                  <Pill key={s}>{s}</Pill>
                ))}
              </div>
            </motion.div>
          ))}
        </div>
      </Section>

      {/* Experience */}
      <Section id="experience" title="Experience">
        <div className="space-y-10">
          {experience.map((job) => (
            <div key={job.company} className={`relative pl-8 md:pl-16 border-l-2 border-[rgb(${BLUEBONNET_700_RGB})]/50`}>
                {/* Timeline Dot: bluebonnet-500 */}
                <div className={`absolute left-[-11px] md:left-[-15px] top-0 h-5 w-5 rounded-full bg-[rgb(${DEEP_BLUE_GLOW_RGB})] border-4 border-[${SLATE_BG_DARK}] shadow-xl`}></div>
                
                <motion.div 
                    // bluebonnet-700 border (Hoverable)
                    className={`rounded-2xl bg-slate-900/70 p-6 shadow-xl border border-slate-700/50 transition duration-300 cursor-pointer`}
                    whileHover={hoverStyles} // Applied custom hover styles (deep blue glow)
                    initial={{ opacity: 0, x: -20 }}
                    whileInView={{ opacity: 1, x: 0 }}
                    viewport={{ once: true, amount: 0.2 }}
                    transition={{ duration: 0.5 }}
                >
                    <div className="flex flex-col sm:flex-row items-start justify-between gap-4">
                        <div>
                            <h3 className="font-bold text-slate-50 text-xl">{job.role}</h3>
                            {/* bluebonnet-400 text */}
                            <p className={`text-[rgb(${BLUEBONNET_400_RGB})] text-lg`}>{job.company}</p>
                        </div>
                        <span className="text-sm text-slate-400 shrink-0 mt-1 sm:mt-0">{job.period}</span>
                    </div>
                    <ul className="mt-4 list-disc pl-5 text-slate-200 space-y-2 text-sm">
                        {job.bullets.map((b, i) => (
                        <li key={i} dangerouslySetInnerHTML={{ __html: b }} />
                        ))}
                    </ul>
                </motion.div>
            </div>
          ))}
        </div>
      </Section>

      {/* Projects */}
      <Section id="projects" title="Key Projects">
        <div className="grid md:grid-cols-2 gap-8">
          {projects.map((p) => (
            <motion.div 
                key={p.title} 
                // bluebonnet-700 border (Hoverable)
                className={`rounded-2xl border-2 border-[rgb(${BLUEBONNET_700_RGB})]/50 bg-slate-900/70 p-6 shadow-2xl flex flex-col transition duration-300 cursor-pointer`}
                whileHover={hoverStyles} // Applied custom hover styles (deep blue glow)
                initial={{ opacity: 0, y: 10 }}
                whileInView={{ opacity: 1, y: 0 }}
                viewport={{ once: true, amount: 0.2 }}
                transition={{ duration: 0.4 }}
            >
              <div className="flex items-start justify-between gap-4">
                <div>
                  <h3 className="font-extrabold text-slate-50 text-xl">{p.title}</h3>
                  <p className="text-sm text-slate-400 mt-1">{p.date}</p>
                </div>
              </div>
              <p className="mt-3 text-slate-200 flex-grow">{p.summary}</p>
              <div className="mt-4">
                {p.tech.map((t) => (
                  <Pill key={t}>{t}</Pill>
                ))}
              </div>
              <div className="mt-5 flex gap-4 border-t border-slate-700 pt-4">
                {/* bluebonnet-300 text */}
                {p.links.repo !== "#" && (
                  <a
                    href={p.links.repo}
                    className={`inline-flex items-center gap-2 text-sm text-[rgb(${BLUEBONNET_300_RGB})] hover:text-[rgb(224,231,255)] transition`}
                    target="_blank"
                    rel="noreferrer"
                  >
                    <Github className="h-4 w-4" /> Code Repository
                  </a>
                )}
                {p.links.demo !== "#" && (
                  <a
                    href={p.links.demo}
                    className={`inline-flex items-center gap-2 text-sm text-[rgb(${BLUEBONNET_300_RGB})] hover:text-[rgb(224,231,255)] transition`}
                    target="_blank"
                    rel="noreferrer"
                  >
                    <ExternalLink className="h-4 w-4" /> Live Demo
                  </a>
                )}
              </div>
            </motion.div>
          ))}
        </div>
      </Section>

      {/* Education & Achievements */}
      <Section id="education" title="Education & Certifications">
        <div className="space-y-8">
            
            {/* 1. ACADEMIC BACKGROUND Wrapper (Hoverable) */}
            <motion.div
                className={`rounded-2xl border-2 border-[rgb(${BLUEBONNET_700_RGB})]/50 bg-slate-900/70 p-6 shadow-2xl flex flex-col transition duration-300 cursor-pointer`}
                whileHover={hoverStyles}
                initial={{ opacity: 0, y: 10 }}
                whileInView={{ opacity: 1, y: 0 }}
                viewport={{ once: true, amount: 0.2 }}
                transition={{ duration: 0.4 }}
            >
                <h3 className="text-xl font-semibold text-slate-50 mb-4 flex items-center gap-2 border-b border-slate-700 pb-2">
                    <GraduationCap className={`h-5 w-5 text-[rgb(${BLUEBONNET_400_RGB})]`} /> Academic Background
                </h3>
                {/* Individual education items side-by-side (50% width each on md+) */}
                <div className="grid grid-cols-1 md:grid-cols-2 gap-4"> 
                    {education.map((e) => (
                        <DetailBox 
                            key={e.school}
                            title={e.credential}
                            subtitle={e.school}
                            date={e.year}
                            Icon={GraduationCap}
                        />
                    ))}
                </div>
            </motion.div>

            {/* 2. CERTIFICATIONS Wrapper (Hoverable) */}
            <motion.div
                className={`rounded-2xl border-2 border-[rgb(${BLUEBONNET_700_RGB})]/50 bg-slate-900/70 p-6 shadow-2xl flex flex-col transition duration-300 cursor-pointer`}
                whileHover={hoverStyles}
                initial={{ opacity: 0, y: 10 }}
                whileInView={{ opacity: 1, y: 0 }}
                viewport={{ once: true, amount: 0.2 }}
                transition={{ duration: 0.4 }}
            >
                <h3 className="text-xl font-semibold text-slate-50 mb-4 flex items-center gap-2 border-b border-slate-700 pb-2">
                    <Award className={`h-5 w-5 text-[rgb(${BLUEBONNET_400_RGB})]`} /> Certifications & Advanced Training
                </h3>
                {/* Individual certification items stacked (100% width on all screen sizes) */}
                <div className="grid grid-cols-1 gap-4"> 
                    {certifications.map((c) => (
                        <DetailBox 
                            key={c}
                            title={c}
                            Icon={Award}
                        >
                            <p className="text-slate-400">Relevant skills gained: AI, LLM frameworks, MLOps practices.</p>
                        </DetailBox>
                    ))}
                </div>
            </motion.div>

            {/* 3. INTERNSHIPS & LEADERSHIP Wrapper (Hoverable) */}
             <motion.div
                className={`rounded-2xl border-2 border-[rgb(${BLUEBONNET_700_RGB})]/50 bg-slate-900/70 p-6 shadow-2xl flex flex-col transition duration-300 cursor-pointer`}
                whileHover={hoverStyles}
                initial={{ opacity: 0, y: 10 }}
                whileInView={{ opacity: 1, y: 0 }}
                viewport={{ once: true, amount: 0.2 }}
                transition={{ duration: 0.4 }}
            >
                <h3 className="text-xl font-semibold text-slate-50 mb-4 flex items-center gap-2 border-b border-slate-700 pb-2">
                    <Users className={`h-5 w-5 text-[rgb(${BLUEBONNET_400_RGB})]`} /> Internships & Leadership
                </h3>
                {/* Individual items stacked (100% width on all screen sizes) */}
                <div className="grid grid-cols-1 gap-6">
                    
                    {/* Internships/Training */}
                    <DetailBox 
                        title="Internships & Training"
                        Icon={Code}
                    >
                        <ul className="list-disc pl-5 text-slate-200 text-sm space-y-1">
                            <li>Ignite Club (SQL/Python analytics); Power BI/Excel dashboards; automated reporting.</li>
                            <li>IEEE (Advanced Python, Data Analysis).</li>
                        </ul>
                    </DetailBox>
                    
                    {/* Achievements/Leadership */}
                    <DetailBox 
                        title="Achievements & Leadership"
                        Icon={Shield}
                    >
                        <ul className="list-disc pl-5 text-slate-200 text-sm space-y-1">
                            <li>Event Coordinator, DOT’Z v10.0 (200+ participants)</li>
                            <li>Industrial Visit Coordinator</li>
                            <li>Class Committee Representative</li>
                        </ul>
                    </DetailBox>
                </div>
            </motion.div>
        </div>
      </Section>

      {/* Contact */}
      <Section id="contact" title="Get In Touch">
        <motion.div 
            // bluebonnet-700 border (Hoverable)
            className={`rounded-2xl border border-[rgb(${BLUEBONNET_700_RGB})]/50 bg-slate-900/70 p-8 shadow-xl max-w-2xl mx-auto text-center transition duration-300 cursor-pointer`}
            whileHover={hoverStyles} // Applied custom hover styles (deep blue glow)
        >
          <p className="text-slate-200 mb-6 text-lg">I'm currently seeking roles focused on LLMOps and GenAI engineering. Let's connect and discuss how I can drive scalable AI solutions in your team.</p>
          <div className="flex flex-col sm:flex-row items-center justify-center gap-4">
            <a
              href={`mailto:${profile.email}`}
              // bluebonnet-600 button, bluebonnet-500 shadow
              className={`inline-flex items-center gap-2 rounded-xl bg-[rgb(79,70,229)] text-white px-5 py-3 text-base font-semibold shadow-xl shadow-[rgb(${DEEP_BLUE_GLOW_RGB})]/40 hover:bg-[rgb(${BLUEBONNET_700_RGB})] transition w-full sm:w-auto justify-center`}
            >
              <Mail className="h-5 w-5" /> Email Me
            </a>
            <a
              href={profile.links.linkedin}
              // bluebonnet-300 text, bluebonnet-400 hover border
              className={`inline-flex items-center gap-2 rounded-xl border border-slate-600 text-[rgb(${BLUEBONNET_300_RGB})] px-5 py-3 text-base hover:bg-slate-800 hover:border-[rgb(${BLUEBONNET_400_RGB})] transition w-full sm:w-auto justify-center`}
              target="_blank"
              rel="noreferrer"
            >
              <Linkedin className="h-5 w-5" /> LinkedIn Profile
            </a>
          </div>
          <p className="text-sm text-slate-500 mt-4">
            <Phone className="h-4 w-4 inline mr-2" /> {profile.phone}
            <span className="mx-2">|</span>
            <MapPin className="h-4 w-4 inline mr-2" /> {profile.location}
          </p>
        </motion.div>
      </Section>

      <footer className="py-12 text-center text-sm text-slate-500 border-t border-slate-800 mt-12">
        <p>
          © {new Date().getFullYear()} {profile.name}. Designed with a focus on AI/LLM expertise.
        </p>
      </footer>
    </div>
  );
}